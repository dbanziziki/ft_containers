#include "stack.hpp"
#include <deque>
#include <map>

int main() {
	int i = 5;
	for (int i = 0; i < 5; ++i)
	{
		std::cout << i << std::endl;
	}
	ft::stack<int> s;
	ft::stack<int> s1;
	std::map<int, int> m;

	s.push(5);
}
